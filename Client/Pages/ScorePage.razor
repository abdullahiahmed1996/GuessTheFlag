@page "/score"
@inject IUserService userService
@inject NavigationManager navManager


<div class="main-background">
    <img class="backgroundpic" src="Images/Background.jpg" ); alt="backgroundpic" />
    <div class="blue-box text-center">
        <h1 class="gtfHeader text-white">Your Score:</h1>
        <br />
       <div class="print-points">
            <label class="text-white">
                Points: @score
            </label>
       </div>
        <div class="buttons">
            <button @onclick="PlayAgain" type="button" class="btn-play btn-light ">
                Play again
            </button>
            <br />
            <button @onclick="StopPlaying" type="button" class="btn-stopplaying btn-light mt-3">
                Stop playing
            </button>
        </div>
    </div>
</div>



@code {
    private int score;
    private string username; 


    protected override async Task OnInitializedAsync()
    {
        var uri = new Uri(navManager.Uri);
        var queryString = System.Web.HttpUtility.ParseQueryString(uri.Query);
        username = queryString["username"];
        score = int.Parse(queryString["score"]);
    }


    private void PlayAgain()
    {
        navManager.NavigateTo("/guesstheflag"); // navigera till startsidan guess the flag



        // restart game here,  
        // go to play page  
    }

    private void StopPlaying()
    {
        navManager.NavigateTo("/");

        // stop playing, open new slide or move to the head slide, index sidan.
        // Reset game
    }

    // hämta score från databas och displaya. metod.
}
